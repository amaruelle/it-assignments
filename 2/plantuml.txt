@startuml
!theme mono
start
:Инициализация max_value = 0;

repeat
  :Ввод числа;
  :Попытка преобразовать в float;
  :Проверка на исключение;
  if (Ошибка?) then (да)
    :Вывод сообщения об ошибке;
  else (нет)
    :Вычисление абсолютного значения;
    if (Новое число больше максимального на данный момент?) then (да)
      :Обновление максимального;
    endif
  endif
repeat while (количество чисел < 10)

:Вывод max_value;
stop
@enduml

